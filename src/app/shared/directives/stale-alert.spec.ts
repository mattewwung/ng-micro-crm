import { StaleAlert } from './stale-alert';

describe('StaleAlert', () => {
  it('should create an instance', () => {
    const directive = new StaleAlert();
    expect(directive).toBeTruthy();
  });
});
